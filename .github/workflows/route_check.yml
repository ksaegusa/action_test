name: issue driven
on: 
  issue_comment:

jobs:
  issue_commented:
    name: Issue comment
    if: github.event_name == 'issue_comment' &&
        startsWith(github.event.comment.body, '/subnet_get')
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Comment on issue #${{ github.event.issue.number }}"
